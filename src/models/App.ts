/**
 * Creates the app object - this gets referenced in any file we want to add / game components to
 */
import { Application } from "pixi.js";
const appProps = {
    theApp : new Application()
};

export default appProps